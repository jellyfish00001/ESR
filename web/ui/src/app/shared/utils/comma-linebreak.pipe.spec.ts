import { CommaLinebreakPipe } from './comma-linebreak.pipe';

describe('CommaLinebreakPipe', () => {
  it('create an instance', () => {
    const pipe = new CommaLinebreakPipe();
    expect(pipe).toBeTruthy();
  });
});
