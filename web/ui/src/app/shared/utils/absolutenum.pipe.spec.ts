import { AbsolutenumPipe } from './absolutenum.pipe';

describe('AbsolutenumPipe', () => {
  it('create an instance', () => {
    const pipe = new AbsolutenumPipe();
    expect(pipe).toBeTruthy();
  });
});
